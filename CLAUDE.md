# カラーピッカーアプリ 完全設計書（最終版）
## コード君への実装指示プロンプト

---

## プロジェクト概要

グラフィックデザイナー・DTPデザイナー向けのカラー管理アプリ。
Mac（Electron）とiPhone（PWA）の両対応。リアルタイム同期あり。
クローズドベータとして招待制で配布予定。

---

## 技術スタック

| 項目 | 技術 |
|------|------|
| Mac | Electron + React |
| iPhone | PWA（React、Service Worker） |
| バックエンド | Supabase |
| 認証 | Google OAuth（Supabase Auth） |
| オフライン | Service Worker + IndexedDB キャッシュ |
| スタイル | Tailwind CSS |
| 言語 | TypeScript |

---

## DB設計（Supabase）

### 基本方針
- 保存はHEX・alpha・CMYKのみ
- RGB・HSLは表示時に都度計算（DB保存しない）
- CMYKは印刷用途のため手動入力・手動保存（計算値と別管理）
- 全テーブルにRLS（Row Level Security）を適用：`user_id = auth.uid()`
- インデックス必須：user_id / folder_id / tag_id / hex / updated_at / is_favorite / is_archived

### テーブル：colors
| カラム | 型 | 説明 |
|--------|-----|------|
| id | uuid | PK |
| user_id | uuid | FK → auth.users |
| folder_id | uuid | FK → folders（nullable） |
| hex | text | #RRGGBB |
| alpha | float | 0.0〜1.0（透明度） |
| c, m, y, k | float | 印刷用CMYK（手動入力・nullable） |
| cmyk_source | text | CMYKの入力元：manual / converted / print_spec |
| name | text | 色の名前（nearest-colorで自動生成・ユーザーが上書き可） |
| spot_color | text | 特色メモ（例：PANTONE 286 C / DIC-43） |
| memo | text | 一言メモ |
| is_locked | bool | 色単位ロック |
| is_favorite | bool | お気に入り |
| is_archived | bool | アーカイブ（削除せず非表示） |
| order | int | 並び順（ドラッグ並び替え用） |
| used_count | int | コピー時に+1（使用頻度カウント） |
| last_used_at | timestamp | 最後にコピーした日時 |
| created_at | timestamp | |
| updated_at | timestamp | |

**同一色の扱い**
同じHEXが追加された場合は重複作成せず、updated_atを更新してリスト最上部に移動する。

**used_countの更新ルール**
- コピーボタンを押したとき：+1
- 詳細パネルを開いただけ：加算しない
→「見た色」ではなく「実際に使った色」を記録するため

### テーブル：folders
| カラム | 型 | 説明 |
|--------|-----|------|
| id | uuid | PK |
| user_id | uuid | FK → auth.users |
| name | text | フォルダ名 |
| is_locked | bool | フォルダ単位ロック |
| order | int | 並び順（ドラッグ並び替え用） |
| created_at | timestamp | |

### テーブル：tags
| カラム | 型 | 説明 |
|--------|-----|------|
| id | uuid | PK |
| user_id | uuid | FK → auth.users |
| name | text | タグ名 |

### テーブル：color_tags
| カラム | 型 | 説明 |
|--------|-----|------|
| color_id | uuid | FK → colors |
| tag_id | uuid | FK → tags |

### テーブル：color_history（ローカルのみ・Supabase不使用）
| カラム | 型 | 説明 |
|--------|-----|------|
| id | uuid | PK |
| hex | text | |
| alpha | float | |
| created_at | timestamp | |

**履歴の仕様**
- 保存先はIndexedDB（端末ローカルのみ・Supabaseには同期しない）
- 最新50件を保持（FIFO：古いものから自動削除）
- 同一HEXは上書き（重複作成しない）
- 「保存」ボタンを押した色だけSupabaseに同期してコレクションに追加

### テーブル：invitations（クローズドベータ管理）
| カラム | 型 | 説明 |
|--------|-----|------|
| id | uuid | PK |
| code | text | 招待コード |
| used_by | uuid | FK → auth.users（nullable） |
| used_at | timestamp | |
| created_at | timestamp | |

---

## 機能一覧

### カラー取得
- 画像アップロードからスポイト抽出（1色ずつ）
- 画像から主要色を自動抽出・一括追加（パレット生成：上位5色）
- 画面からスポイト抽出（Macのみ・Electron API使用）
- カラーコード直接入力（HEX / RGB / HSL）
- 取得した色は自動でローカル履歴に保存

### カラー管理
- 対応フォーマット表示：HEX / RGB / RGBA / HSL / HSLA / CMYK
- 透明度（Alpha）対応：RGBAおよびHSLAでの保存・コピー
- CMYKは手動入力・保存（印刷用正式値として管理）。入力元をcmyk_sourceで記録
- 色の名前：nearest-colorで自動生成はプレースホルダー扱い。リスト上で直接クリックして自由に上書き編集できる
- 特色メモ欄（PANTONE / DIC番号などを自由テキストで入力・検索可）
- 一言メモ
- タグ（複数付与可・横断検索可）
- お気に入り（星マーク）
- ロック機能（色単位）：ロック中は編集・削除不可
- アーカイブ：削除せずグレーアウト表示。「アーカイブ非表示」フィルターで普段は隠せる
- コピーボタン：デフォルトフォーマットでコピー（ユーザー設定で固定可。HEX / RGB / RGBA / HSL / CMYKから選択）
- フォーマット個別コピー：詳細パネルで各フォーマットごとにコピーボタンあり
- 削除ボタン：ロック中は非活性
- ドラッグ＆ドロップで並び替え
- Undo / Redo（⌘+Z / ⌘+Shift+Z）：削除・移動・バルク操作など全操作に対応
- バルク操作：複数選択して一括タグ付け・一括フォルダ移動・一括削除

### 印刷補助機能
- **総インク量チェック（TAC値）**：CMYKの合計が印刷所上限（300〜320%）を超えたら警告表示
- **色域警告（ガマット警告）**：RGBで作った色がCMYK再現範囲外の場合に警告表示
- **特色メモ**：PANTONE / DIC番号を自由テキストで保存・検索可能

### フォルダ機能
- 任意のフォルダ作成・削除・リネーム
- ドラッグ＆ドロップで並び替え
- フォルダ単位ロック：ロック中は中の色も編集・削除不可
- フォルダ単位でパレット書き出し（CSV / JSON / Adobe ASE）
- 外部ファイルのインポート：ASE / JSON / CSV形式に対応。サイドバーのフォルダメニューまたは設定画面から実行

### ビジュアル書き出し（イラレ・コンセプトシート用）
- フォルダ単位で書き出す
- レイアウト固定：縦並び1行ごとに「丸アイコン ＋ カラーコード ＋ 色名」
  ```
  ● #3A7BD5   コーラルブルー
  ● #F2C94C   サンイエロー
  ● #27AE60   フォレストグリーン
  ```
- 色名はnearest-colorで自動生成（日本語・英語対応）
- 出力形式：SVG / PNG / 両方ZIP
- PNGは解像度選択（72 / 150 / 300dpi）
- アイコンサイズ選択：64px / 128px / 256px
- カラーコードのフォーマット選択：HEX / RGB / RGBA / HSL / CMYK（ラジオボタンで1つ選択）
- SVGはベクターなのでイラレでの拡大縮小も劣化なし

### 全データエクスポート
- ユーザーの全カラーデータをJSON形式で一括書き出し
- フォルダ構造・タグ・メモ・CMYK値を含む完全なバックアップ
- 設定画面から実行可能

### 表示モード
**リストビュー（デフォルト）**
- 色の丸アイコン＋名前＋HEX＋コピー・削除・ロック・お気に入りボタン
- アーカイブ済みはグレーアウトで表示（フィルターで非表示にもできる）
- 各行にアイコンで操作できる

**ギャラリービュー（パレット画面）**
- 全色を丸アイコンで並べる
- デフォルト表示は色相順（Hue）でソートしてグラデーションのように見せる
- 系統別フィルターボタン（赤・橙・黄・緑・青・紫・ピンク・無彩色）
- フィルターボタンを押すとその系統だけ表示
- タップ／クリックで詳細パネル表示

### 丸アイコン仕様
- 全画面共通で丸表示に統一
- サイズ：リストビューは40px、ギャラリービューは56px
- 透明度がある色はチェッカー柄の上に色を重ねて表現
- 選択中の色はリング表示でハイライト
- 白/黒背景プレビュー切り替えボタン（詳細パネルに配置）

### カラージェネレーター
- 色を1〜複数選んで配色提案を生成
- 配色パターン：補色 / 類似色 / トライアド / テトラード / スプリット補色
- パターン名で選択できるUI（AdobeColor風）
- 生成結果からワンクリックで保存

### コントラストチェッカー・色覚シミュレーション
- 2色を選んでWCAGコントラスト比を表示
- AA / AAAの合否判定を表示
- 読めるテキスト色の自動提案
- 色覚多様性シミュレーション：P型（赤色弱）/ D型（緑色弱）/ T型（青色弱）/ グレースケールの見え方を詳細パネル内でプレビュー

### 履歴
- ピッカーで取得した色を自動保存（最新50件・FIFO）
- 保存先はIndexedDB（端末ローカルのみ・Supabaseには同期しない）
- 「最近使った色」エリアとしてサイドバーに常時表示
- 保存ボタンを押した色だけSupabaseに同期してコレクションに追加

### 検索・絞り込み
- 色名・HEX・メモ・タグ・特色メモでテキスト検索（⌘+F）
- タグで絞り込み
- お気に入りのみ表示
- 使用頻度順（used_count）で並び替え可
- アーカイブ表示 / 非表示切り替え

### 同期
- Mac ↔ iPhone リアルタイム同期（Supabase Realtimeをフォルダ単位・差分更新で最適化）
- オフライン時はIndexedDBにキャッシュ、復帰時に自動同期
- 競合解決：updated_atで最後勝ち

### ユーザー設定
- UI言語：日本語 / English（切り替え可）
- 色名の言語：日本語 / English（UI言語と独立して選択可）
- デフォルトコピーフォーマット：HEX / RGB / RGBA / HSL / CMYKから選択

### セキュリティ・運用
- 全テーブルにRLS適用（user_id = auth.uid()）
- クローズドベータ：招待コード制。アプリ内に「Beta版・再配布禁止」を明記
- 保存数上限：1ユーザーあたり500色まで（正式版では1000〜無制限を検討）

---

## UI構成

```
├── サイドバー
│   ├── 検索バー
│   ├── すべての色
│   ├── お気に入り
│   ├── 最近使った色（履歴）
│   ├── フォルダ一覧（追加・ドラッグ並び替え可）
│   ├── タグ一覧
│   └── カラージェネレーター
│
├── メインエリア
│   ├── ビュー切り替えタブ（リスト / ギャラリー）
│   ├── フィルターバー（系統別・タグ別・アーカイブ）
│   ├── リストビュー
│   │   └── 各行：丸アイコン・名前・HEX・コピー・削除・ロック・★
│   └── ギャラリービュー
│       └── 丸アイコングリッド（色相順）
│
├── 詳細パネル（右サイドまたはモーダル）
│   ├── 丸アイコン（大）＋白/黒背景切り替えボタン
│   ├── 色名（クリックで直接編集）
│   ├── 各フォーマット表示＋個別コピーボタン（HEX/RGB/RGBA/HSL/HSLA/CMYK）
│   ├── 透明度スライダー
│   ├── CMYK手動入力欄＋cmyk_source表示＋TAC値＋色域警告
│   ├── 特色メモ欄（PANTONE / DIC等）
│   ├── 一言メモ欄
│   ├── タグ
│   └── コントラストチェッカー
│
└── ピッカーオーバーレイ
    ├── 画像アップロード＋スポイト（1色）
    ├── 画像から主要色を一括抽出（上位5色）
    └── スクリーンピッカー（Macのみ）
```

---

## ショートカットキー（Mac・Electron）

| 操作 | ショートカット |
|------|--------------|
| アプリをグローバル呼び出し | `⌘ + Shift + P` |
| スクリーンピッカー起動 | `⌘ + Shift + C` |
| 新規カラー追加 | `⌘ + N` |
| 新規フォルダ作成 | `⌘ + Shift + N` |
| 選択色をコピー（デフォルト形式） | `⌘ + C` |
| 選択色を複製 | `⌘ + D` |
| 選択色を削除 | `⌘ + Delete` |
| Undo | `⌘ + Z` |
| Redo | `⌘ + Shift + Z` |
| 検索 | `⌘ + F` |
| カラージェネレーター | `⌘ + G` |
| リスト / ギャラリー切り替え | `⌘ + 1 / ⌘ + 2` |

---

## デザイン方針

- ダークモード専用（ライトモードなし）
- 色の丸アイコンを全画面で統一
- シンプル・ミニマルなUI。余計な装飾なし
- 色そのものが主役になるデザイン
- Apple品質のポリッシュ感を目標

---

## 実装推奨順序

1. Supabase・Google認証・RLS・DB設計・招待コードテーブル
2. 基本UI（サイドバー・メインエリア・詳細パネル）
3. カラー管理CRUD・フォルダ機能・ドラッグ並び替え
4. リストビュー・ギャラリービュー・アーカイブ・バルク操作
5. カラー取得（画像スポイト・画像パレット一括抽出・スクリーンピッカー）
6. ショートカットキー・ロック機能・お気に入り
7. タグ・特色メモ・一言メモ・検索・絞り込み
8. 印刷補助（TAC値チェック・色域警告・cmyk_source）
9. カラージェネレーター
10. コントラストチェッカー・色覚シミュレーション・ビジュアル書き出し（SVG/PNG）・パレット書き出し・インポート・全データエクスポート
11. 履歴（IndexedDBローカル）・透明度・白黒背景プレビュー・used_count・Undo/Redo
12. オフライン対応（Service Worker + IndexedDB）
13. PWA化（iPhone対応）

---

## 意思決定ログ（採用しなかった機能と理由）

複数のAIレビューを経て検討したが、以下は意図的に除外した。

| 項目 | 除外理由 |
|------|---------|
| color_profile（sRGB/P3/AdobeRGB） | 実装コスト過多・現場での運用実態と乖離 |
| version（楽観的ロック） | 同時編集のユースケースが限定的・過剰設計 |
| lock_type（編集/削除の分離） | UX複雑化の割にメリット薄い |
| CSS Variables / トークン出力 | ターゲット外（エンジニア向け）・コンセプトのブレ |
| カラーミックス（中間色生成） | カラージェネレーターで代替可能・v2以降で検討 |
| 自動タグ（warm/cool等） | タグ機能で代替可能・複雑化リスク |
| tsvector（全文検索） | 現時点の規模では不要 |
| iPhoneカメラスポイト | v2以降で検討 |
| ドラッグ&ドロップでイラレに直接 | 実装重い・v2以降で検討 |
| 用紙別プレビュー | 精度が出しにくい・v2以降で検討 |

---

## 備考

- 配布形式：Mac = DMGファイル直配布、iPhone = URL共有（PWA）
- ユーザーデータはSupabaseで一元管理（ユーザーごとに分離・RLS適用）
- クローズドベータ：招待コード制・再配布禁止をアプリ内に明記
- 将来的にApp Store申請を想定した構成にしておく

---

## 実装時の補足仕様（AIが迷わないための定義）

### レスポンシブの境界
- Tailwindのブレイクポイント `md: 768px` を基準とする
- モバイル（iPhone PWA）時はサイドバーをハンバーガーメニューに格納する
- Mac（Electron）時は常にサイドバーを表示する

### オフライン時のエラーUI
- ネットワーク切断中にSupabaseへの保存を試みた場合、トースト通知で表示する
- 表示内容：「ローカルに保存しました。オンライン復帰時に自動で同期されます」
- 復帰時は自動同期し、完了時にも軽くトースト通知を出す

### カラーコードのバリデーション
- HEX / RGB / HSL等の直接入力時、不正な値はリアルタイムで検知する
- 不正な値の場合：入力フィールドの枠線を赤くする
- 保存ボタンは不正な値がある間は非活性化する
