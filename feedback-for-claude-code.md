# コード君への指示書（ブラッシュアップ版）

作成日: 2026-04-11
更新者: ゆうた

---

## 📋 現在の進捗

ステップ 1〜13（CLAUDE.md 推奨順序）はすべて完了。
P2バックログの作業中。特に「タグ管理インライン編集・削除UI」は今セッション完了済み。

詳細な実装状況は別紙「【カラーピッカーアプリ 実装状況レポート】」を参照。

---

## 🎨 次フェーズの優先順位判断

以下の 3 タスク群から、**実装効率・ボリューム・既存ロジックへの影響を考えて、優先度を自分で判断して進めてください。**

ゆうたさんは「どれから？」という相談には応じず、コード君が「これが先に片付いた方があとが楽」と判断した順序で OK です。

---

## Task 1: UIブラッシュアップ（優先度：判断にゆだねる）

**何をするか**

現在のカラー定義（フォント・背景・アクセント）をすべて維持しつつ、「質感」と「メリハリ」を整理します。前の提案（ガラスモーフィズム全盛）から「引き算」に改善しました。

**スコープ**

### ① 境界線の整理（1px Border化）

全体の「キレ」を出します。

- **カラム間・要素間** に影ではなく **`1px` の精密なボーダー** を導入
  - ダークモード: `border-color: rgba(255,255,255,0.1)`
  - ライトモード: `border-color: rgba(0,0,0,0.05)`
- 現在の影（`shadow`）を段階的に削除し、ボーダーへ置き換え
- DetailPanel の区切り線も同様に 1px ボーダーに統一

### ② ポップアップのガラス化

**浮き上がる要素のみ** に `backdrop-filter: blur(12px)` を適用。

対象：
- メニュー（AddColor / ExportMenu / ImportMenu 等）
- モーダル（ウィンドウ上の薄いカバー）
- ドロップダウン（フィルター選択 等）

**適用 しない** ：
- メインのカラムレイアウト
- Sidebar
- DetailPanel
- リスト・ギャラリービュー本体

### ③ Glowの微調整

**既存仕様を維持** しつつ、明度・色をさらに控えめに。

- アクセントカラーが使われているUI要素（アクティブなタブ、ボタン等）にのみ適用
- **カラーチップや丸アイコン自体には適用しない** ← 重要
- グロー値（`box-shadow` 拡散度）を現在より 20% 薄く

### ④ アニメーション（キビキビとした動き）

framer-motion の Spring Config を以下に統一：

```javascript
{ stiffness: 400, damping: 25 }
```

適用対象：
- フォルダの開閉
- パネルの出し入れ
- 要素の追加・削除時の Stagger（`0.05s` ずつ遅延）

無駄な揺れを抑えた「小気味よい反応」を追求します。

**判断理由**

「何でも変えられる」から一歩進め、**「重要なところだけが特別（ガラス・光）」** というメリハリをつけることで、デザインのノイズを減らしつつ高級感を維持します。

---

## Task 2: 伝統色データソース整理（P2）

**何をするか**

現在の伝統色判定ロジックを見直し、データの正確性と判定精度を上げます。

**現状の問題点**

- `src/lib/japaneseTraditionalColors.ts` がベタ書き
- データ件数が曖昧（整理不足）
- 出典が不明瞭（参考: https://www.i-iro.com/dic/category/jpn ）
- 距離閾値 40 の根拠が薄い
- 間違った色に伝統色名が割り当たるケースがある（閾値40で緩和しているが、本質的な解決ではない）

**スコープ**

### ① データソースの整理

- `japaneseTraditionalColors.ts` を新規ファイル `src/lib/data/japaneseTraditionalColors.ts` に分離
- 各伝統色に以下のメタデータを追加
  ```typescript
  {
    name: string;        // 日本語名
    hex: string;         // HEX値
    source: string;      // 出典（例：「江戸の色」など）
    description?: string; // 簡単な説明
  }
  ```
- データベースの出典を明記（i-iro.com 等）
- 総件数を確認・整理（現在は約 465 色と書いてあるが正確な件数を把握）

### ② 距離閾値の見直し

- 現在の閾値 40 について、実装者が以下を検証：
  - 実際の色サンプルで「これは伝統色として認識できるか」をテスト
  - 必要なら 35 / 45 / 50 など調整
- `getTraditionalColorName()` と同期版 `getTraditionalColorNameSync()` の両方で統一

### ③ フィルターに「伝統色あり」を追加

FilterBar に新しいフィルター選択肢を追加：

- フィルター種類: 「伝統色あり」（boolean toggle）
- フィルターUI: 既存のトーンフィルター（5分類）と同じ階層に配置
- ロジック: `colors.filter(c => c.traditionalColorName !== null)`

**判断理由**

伝統色はカラーピッカーの「品質」を大きく左右します。データソースを整備することで、ユーザーへの信頼感が格段に上がります。

---

## Task 3: カラージェネレーター入力強化（P2）

**何をするか**

現在の「1色を選んで配色パターンを選ぶ」から、より柔軟な入力方法へ拡張します。

**現状**

- 入力: 単一の色 + パターン選択（補色/類似色 等）
- 出力: 該当パターンの色セット

**拡張内容**

### ① 複数色起点の配色生成

- UI: 入力エリアに「+色を追加」ボタンを追加
- 機能: 複数の色を起点にして、それらの中点色やテーマを自動生成
- 実装例：
  ```typescript
  generatePaletteFromMultiple(baseColors: Color[], pattern: 'harmony' | 'contrast' | 'blend'): Color[]
  ```

### ② ベース色の色相スライダー直接操作

- UI: メイン入力色の下に「色相スライダー」を追加
- 機能: スライダーをドラッグすると、生成パターンもリアルタイムに変わる
- エフェクト: Spring アニメーション（上記 Task 1 と同じ config）で色が流れるように変化

### ③ 生成結果のプレビュー改善

- 現在: パターンを選ぶとすぐに色が生成される
- 改善: パターン選択 → プレビューの一覧表示 → 「この組み合わせを使う」を確定
- UI: 「キャンセル」と「確定」ボタンを用意

**UIが複雑になりすぎる vs シンプルさのバランス**

- 実装者の判断で、段階的に追加することを推奨
- 第 1 段: 複数色起点のみ実装
- 第 2 段: スライダー追加
- 第 3 段: プレビューを精整
- 各段で「動くことを確認」→「ゆうたに見せる」という流れでOK

---

## 📌 技術的負債・懸念点

以下の点を、余裕があれば対応してください。優先度は「UI刷新 > 伝統色 > ジェネレーター」の後に。

### ① 型安全性の向上

```typescript
// 現在: Supabase 型推論エラーを any で回避
const db = supabase as any;

// 改善: supabase-js v2 の型を正しく活用
const { data, error } = await supabase
  .from('colors')
  .select('*')
  .eq('user_id', userId);
```

- `npx supabase gen types typescript` を実行し、型定義を自動生成
- colorStore / folderStore / tagStore / historyStore で `as any` を排除

### ② DRY化の継続

- `getHueCategory()` が GalleryView / FilterBar / AppLayout で重複しているので、共有化済み
- 他に重複ロジックがあればさらに共有化

---

## 🎯 実装の進め方

**重要：** 以下のルールを守ってください。

1. **1 つのタスクを完全に実装してからゆうたに見せる**
   - 「境界線の整理」が完全に終わってから「ポップアップガラス化」へ
   - 中途半端な状態で「これ、どう？」と聞かない

2. **各タスク完了後に PR を作成**
   - ブランチ例: `feature/ui-refresh-borders`
   - PR タイトル：「UIブラッシュアップ: 境界線を 1px ボーダーに統一」

3. **ゆうたさんがレビュー → マージ → 次のタスク開始**

4. **わからないことは「実装前に」聞く**
   - 実装してからの質問はNG
   - 例：「ダークモードで `rgba(255,255,255,0.1)` は実際に見えるか不安」← 実装前に相談してOK

---

## 📞 質問がある場合

以下の形式で相談してください：

```
【Task】: Task 1 / Task 2 / Task 3 のどれか
【質問】: 具体的に何が不明か
【案】: コード君の推測
【ゆうたへのお願い】: どの判断を待ってるか
```

例）
```
【Task】: Task 1 - ポップアップガラス化
【質問】: blur(12px) で「浮いている」感じが出るか確信がない
【案】: 実装して実際に見てみます
【ゆうたへのお願い】: 実装後に「これで OK か」の判定をお願いします
```

---

## 📅 完了予想日程

ゆうたさんは指定しません。コード君が **「これなら自分の実装ペースで確実に終わる」** と判断した日程を示してください。

---

これでいきます？
